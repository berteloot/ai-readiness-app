# Ensure yarn is used consistently
# Use npm registry
registry "https://registry.npmjs.org/"

# Enable zero-installs for faster builds
nodeLinker "node-modules"

# Ensure consistent package resolution
resolutionMode "highest"
